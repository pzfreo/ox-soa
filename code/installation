ubuntu

install vm ware tools https://kb.vmware.com/selfservice/microsites/search.do?language=en_US&cmd=displayKC&externalId=1022525
restart

sudo apt-get update
sudo apt-get upgrade
sudo apt-get install docker.io docker-compose
sudo su
curl -L https://github.com/docker/machine/releases/download/v0.7.0/docker-machine-`uname -s`-`uname -m` > /usr/local/bin/docker-machine && \
chmod +x /usr/local/bin/docker-machine

sudo apt-get install openjdk-8-jdk

sudo apt-get install maven
sudo apt-get install chromium-browser

Lock Chromium to the launcher
Install ARC into Chromium 

sudo apt-get install nodejs npm
npm update

sudo add-apt-repository ppa:webupd8team/atom
sudo apt-get update
sudo apt-get install atom
Start atom and lock to launcher

sudo apt-get install python-setuptools python-dev build-essential
sudo easy_install pip
sudo pip install httplib2

sudo npm install -g jasmine-node frisby
sudo ln -s `which nodejs` /usr/bin/node

sudo apt-get install mitmproxy

sudo pip install mitmproxy PySocks

Installed tomcat
renamed to ~/servers/tomcat
chmod +x bin/*.sh


Downloaded sample-service.war

Downloaded and installed SOAP UI

CXF 2.7.18

6)	Set JAVA_HOME in .bashrc
export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64

7)	Also CXF_HOME and Path in .bashrc
export CXF_HOME=~/servers/apache-cxf-2.7.18
export PATH=$PATH:~/servers/apache-cxf-2.7.18/bin


8)	Apache Maven 3.1.3 or later*
Apache Ant 1.9.3 or later*
Curl 7.35.0 or later*
Tree*

sudo apt-get install ant maven curl tree


sudo nano /etc/security/limits.conf

sudo nano /etc/security/limits.conf
#<domain>      <type>  <item>         <value>
#
*                soft    nproc         60000
*                hard  nofile         10000
*                soft    nofile         10000



Eclipse Kepler JEE
set eclipse view to filter out external libraries on the project explorer. 

 mvn -Declipse.workspace=/home/oxsoa/workspace eclipse:configure-workspace

Set default code level to 1.7 or higher

https://askubuntu.com/questions/80013/how-to-pin-eclipse-to-the-unity-launcher

http://stackoverflow.com/questions/23011547/webservice-client-generation-error-with-jdk8

sudo add-apt-repository ppa:cwchien/gradle
sudo apt-get update
sudo apt-get install gradle-2.13

sudo add-apt-repository ppa:chris-lea/redis-server
sudo apt-get update


Unfortunately we need to fix something inside Ubuntu before we can do this lab. 

In a terminal window:
sudo nano  /etc/NetworkManager/NetworkManager.conf

Then make sure the dnsmasq line is commented out with a #:

[main]
plugins=ifupdown,keyfile,ofono
#dns=dnsmasq

[ifupdown]
managed=false

Finally, restart the network manager:

sudo restart network-manager

sudo apt-key adv --keyserver hkp://p80.pool.sks-keyservers.net:80 --recv-keys 58118E89F3A912897C070ADBF76221572C52609D
echo "deb https://apt.dockerproject.org/repo ubuntu-xenial main" | sudo tee /etc/apt/sources.list.d/docker.list
sudo apt-get update
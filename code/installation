ubuntu

install vm ware tools https://kb.vmware.com/selfservice/microsites/search.do?language=en_US&cmd=displayKC&externalId=1022525
restart

sudo apt-get update
sudo apt-get upgrade
sudo apt-get install docker.io docker-compose
sudo su
curl -L https://github.com/docker/machine/releases/download/v0.7.0/docker-machine-`uname -s`-`uname -m` > /usr/local/bin/docker-machine && \
chmod +x /usr/local/bin/docker-machine

sudo apt-get install openjdk-8-jdk

sudo apt-get install maven
sudo apt-get install chromium-browser

Lock Chromium to the launcher
Install ARC into Chromium 

sudo apt-get install nodejs npm
npm update

sudo add-apt-repository ppa:webupd8team/atom
sudo apt-get update
sudo apt-get install atom
Start atom and lock to launcher

sudo apt-get install python-setuptools python-dev build-essential
sudo easy_install pip
sudo pip install httplib2

sudo npm install -g jasmine-node frisby
sudo ln -s `which nodejs` /usr/bin/node
